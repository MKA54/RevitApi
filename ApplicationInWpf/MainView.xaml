<Window x:Class="ApplicationInWpf.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApplicationInWpf"
             mc:Ignorable="d" 
             Height="350" Width="400"
             Title="Создание воздуховода">
    <StackPanel>
        <Label Content="Выбрать тип воздуховода" Margin="5" />
        <ComboBox ItemsSource="{Binding DuctTypes}"
                  SelectedValue="{Binding SelectedDuctType}"
                  DisplayMemberPath="Name"
                  Margin="5"/>
        <Label Content="Выбрать уровень" Margin="5" />
        <ComboBox ItemsSource="{Binding Levels}"
                  SelectedValue="{Binding SelectedLevel}"
                  DisplayMemberPath="Name"
                  Margin="5"/>
        <Label Content="Смещение воздуховода" Margin="5" />
        <TextBox Text="{Binding Displacement}" Margin="5" />

        <Button Content="Применить" Command="{Binding SaveCommand}" Margin="5" />
    </StackPanel>
</Window>